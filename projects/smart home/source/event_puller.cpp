#include "event_puller.hpp"

namespace smartHome{

/* EventPuller::EventPuller(EventBus& a_safeQueue)
    : m_safeQueue(a_safeQueue)
{
}

void EventPuller::PullEvent(SharedPtr<Event>& a_event) {
    m_safeQueue.Dequeue(a_event);
}

size_t EventPuller::Size() const NOEXCEPTIONS{
    return m_safeQueue.Size();
}  */

}//smartHome