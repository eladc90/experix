#include "event_pusher.hpp"

namespace smartHome{


/* EventPusher::EventPusher(EventBus& a_safeQueue)
    :m_safeQueue(a_safeQueue)
{
}

void EventPusher::PushEvent(SharedPtr<Event> a_event) {
    m_safeQueue.Enqueue(a_event);
} */

}//smartHome